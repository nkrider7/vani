{
  "name": "vani-cycle",
  "version": "0.1.2",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsup",
    "dev": "ts-node-dev --respawn src/examples/dev.ts",
    "lint": "eslint --ext .ts src",
    "test": "jest --coverage",
    "prepare": "npm run build",
    "prepublishOnly": "npm run test && npm run build"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^29.0.0",
    "ts-node-dev": "^2.0.0",
    "tsup": "^6.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "date-fns": "^2.30.0"
  }
}
